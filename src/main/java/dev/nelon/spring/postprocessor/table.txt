1. BeanDefinitionRegistryPostProcessor
   └── postProcessBeanDefinitionRegistry()

2. BeanFactoryPostProcessor
   └── postProcessBeanFactory()

====== НАЧИНАЕТСЯ СОЗДАНИЕ БИНОВ ======

3. InstantiationAwareBeanPostProcessor
   └── postProcessBeforeInstantiation()

4. Constructor (new)

5. InstantiationAwareBeanPostProcessor
   └── postProcessAfterInstantiation()

6. Dependency Injection (@Autowired, @Value)
   └── postProcessProperties()

7. BeanPostProcessor
   └── postProcessBeforeInitialization()

8. @PostConstruct / InitializingBean

9. BeanPostProcessor
   └── postProcessAfterInitialization()

🧠 Мнемоника:
Definition → Factory → Instantiation → Initialization → Proxy

Как сказать на собеседовании
Spring Container сначала регистрирует BeanDefinition.
Затем BeanDefinitionRegistryPostProcessor и BeanFactoryPostProcessor
могут изменить метаданные бинов.
После этого начинается создание объектов, где
InstantiationAwareBeanPostProcessor может вмешаться ещё до new и во время DI.
После создания бина BeanPostProcessor используется для инициализации
и создания прокси (AOP, Transactional).